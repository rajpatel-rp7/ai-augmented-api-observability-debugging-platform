# Observability Dashboard

React + TypeScript MVP for browsing services, incidents, and AI analysis.

## Local development

```bash
cd frontend
npm install
npm run dev
```

Set `VITE_API_BASE_URL` if the API is not at `http://localhost:8000`.

## Docker Compose

```bash
docker compose --profile demo up -d frontend
```

Dashboard: http://localhost:5173
